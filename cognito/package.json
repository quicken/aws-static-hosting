{
  "name": "react-hosting",
  "version": "1.0.6",
  "description": "Lambda@Edge authorization function for Cognito-protected React apps",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.js",
    "test": "vitest",
    "test:run": "vitest run",
    "package": "npm run build && mkdir -p bin && cd dist && zip -r ../bin/lambda-$(node -p \"require('../package.json').name\")-$(node -p \"require('../package.json').version\").zip index.js"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "jwks-client": "^2.0.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/jsonwebtoken": "^9.0.7",
    "@types/node": "^24.3.1",
    "esbuild": "^0.25.10",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
